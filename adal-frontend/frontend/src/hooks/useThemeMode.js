// Re-export the canonical hook from ThemeContext to keep a single source of truth.
export { useThemeMode } from "../contexts/ThemeContext";
export { useThemeMode as default } from "../contexts/ThemeContext";
